# Chatbox++ 变更日志

所有版本变更记录。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [1.3.0] - 2026-07-26

初始版本发布。基于 Electron（electron-vite + TypeScript + React + Zustand）构建的桌面 AI 对话应用，通过 OpenAI 兼容 API 与大模型对话，由用户自行提供 API Key。

### 模型管理
- 支持配置多个 OpenAI 兼容格式的模型：名称、API 地址、API Key、模型 ID
- 完整生成参数配置：temperature、top_p、n、presence_penalty、frequency_penalty、max_tokens（留空则使用服务端默认值）
- 模型增删改与列表展示，可设置默认模型
- 默认模型支持「自动（使用上次使用的模型）」：新建对话按「显式指定 → 默认模型 → 上次使用 → 第一个」优先级选择模型

### 思考模式与强度
- 思考模式采用多选勾选（默认 / 开启 / 关闭），由 `thinking.type` 参数控制（默认表示不传入该参数）
- 思考强度支持勾选所有支持的等级：低 / 中 / 高 / 超高 / 极致 / 默认，由 `reasoning_effort` 参数控制（默认表示不传入该参数）
- 两个参数完全独立，可自由组合
- 配置时可选择「思考模式为『默认』时是否允许选择思考强度」：不允许时默认模式始终不传入 `reasoning_effort`
- 配置保存校验：未勾选思考模式或强度时，在对应区域下方显示红字提示

### 对话管理
- 多会话管理：创建、重命名、删除、清空，按更新时间排序
- 新建对话默认隐藏，发送首条消息后才在侧边栏显示并持久化
- 打开应用时自动创建并进入草稿对话，避免空对话堆积
- 草稿对话不写入磁盘，切换模型 / 思考模式 / 对话时自动丢弃
- 无模型时发送消息弹出确认对话框，引导前往设置配置模型

### 对话界面
- 模型选择下拉菜单，收起时仅显示模型名称
- 思考模式下拉菜单：选项为当前模型配置勾选的所有思考模式
- 思考强度下拉菜单：仅思考开启（或默认模式允许）时显示，强度只能在配置勾选范围内选择
- 流式输出与思考过程折叠展示
- 消息复制功能

### API 请求
- 主进程 `net.fetch` 发起流式 SSE 请求
- 思考开启时完整回传所有历史轮次的 `reasoning_content`，确保多轮对话中模型可基于前文思考继续推理
- 响应解析兼容 `reasoning_content` 与 `reasoning` 两种字段命名

### 渲染与显示
- Markdown 渲染（支持 GFM）
- KaTeX 数学公式渲染，支持 `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 四种定界符语法
- 修复公式与 `**` 加粗共存时的渲染问题：预处理时吸收公式外侧空白，避免闭合标记前空格导致配对错位
- 用户输入、AI 回复、思考过程三处均支持公式渲染

### 用户界面
- 简洁美观的图形界面，对新手友好
- 浅色 / 深色 / 跟随系统主题
- 自定义应用图标（1024×1024，紫色渐变 + 对话气泡 + 加号）
- 设置界面分为「通用设置」与「模型管理」两个标签页
- 系统提示词输入框宽度占满设置区域
- 设置弹窗点击外部区域不关闭，避免误触

### 数据与持久化
- 本地 JSON 持久化存储，自动迁移旧数据结构
- 配置数据与对话数据分离存储

### 打包与分发
- 基于 electron-builder 的跨平台打包配置（Windows / macOS / Linux）
- 主进程窗口设置应用图标
- 各平台显式指定打包图标，自动从 PNG 转换为 .ico / .icns
- 采用 MIT 许可证开源
